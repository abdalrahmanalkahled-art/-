# تدقيق حجم سياق الرفع السحابي

## الخلاصة التنفيذية

كان السبب الأوضح القابل للتحسين داخل المشروع هو تكرار الشعار الأصلي خمس مرات بصيغة PNG كاملة بحجم يقارب 692 كيلوبايت لكل نسخة. هذه الملفات كانت متطابقة ثنائياً، وتشمل `icon.png` و`splash-icon.png` و`favicon.png` و`android-icon-foreground.png` ونسخة `logo-new.png` غير المشار إليها في الكود. كما أن قواعد `.easignore` كانت تستبعد معظم الملفات الكبيرة غير المطلوبة، مثل `node_modules` و`docs` و`presentation-assets` و`tests` وملفات النسخ الاحتياطية.

تم ضغط الأصول المطلوبة مع الحفاظ على أبعاد 1024×1024 واستخدام لوحة ألوان مضغوطة من 256 لوناً. كما حُذفت نسخة `logo-new.png` غير المستخدمة، وأضيفت استثناءات آمنة لمجلدات العرض والتطوير المحلي. لم يتغير منطق التطبيق أو إعدادات Expo أو مسارات الأصول المطلوبة.

## القياسات

| القياس | قبل التحسين | بعد التحسين | الفرق |
|---|---:|---:|---:|
| حجم سياق الرفع المحاكى وفق قواعد EAS | 7,170,227 بايت تقريباً | 4,144,623 بايت تقريباً | خفض يقارب 3.03 ميغابايت، أي نحو 42% |
| الأيقونات المطلوبة الأربعة | 2,770,552 بايت | 437,408 بايت | خفض يقارب 2.33 ميغابايت |
| نسخة الشعار غير المستخدمة | 692,638 بايت | 0 | إزالة كاملة |
| الملفات المستثناة من سياق الرفع | تشمل 695 ميغابايت محلياً تقريباً، معظمها من الاعتمادات | ما زالت مستثناة | لا تدخل في الرفع |

الحجم المحسوب هو مجموع الملفات التي تبقى بعد محاكاة قواعد `.easignore`، وليس حجماً نهائياً مضموناً للطلب الشبكي لأن خدمة البناء قد تضيف تغليفاً أو بيانات وصفية خاصة بها.

## ما تم التحقق منه

تمت مراجعة الأبعاد والتطابق الثنائي للأيقونات، ومراجعة مراجع `app.config.ts` وملفات التطبيق. بقيت المسارات المطلوبة كما هي: `icon.png` و`splash-icon.png` و`favicon.png` و`android-icon-foreground.png`. كما تم تشغيل اختبار سياق EAS ونجح بثلاثة اختبارات.

تم تشغيل تصدير Android المحلي بعد التعديل ونجح، وبقيت حزمة Hermes الناتجة بحجم يقارب 11 ميغابايت. كما نجح بناء الخادم، ونجحت 154 مجموعة اختبار غير شبكية بإجمالي 574 اختباراً ناجحاً واختبار واحد متجاوز. اختبار Gemini الشبكي الوحيد انتهى بمهلة زمنية، وهو فشل اتصال خارجي لا يرتبط بتحسين الأصول.

## تقييم الملفات غير الضرورية

| العنصر | الحالة | القرار |
|---|---|---|
| `docs/` وملف دليل الاستخدام | غير مطلوبين لبناء التطبيق | مستثنيان عبر `.easignore` |
| `presentation-assets/` و`presentation-project/` | أصول عرض وليست مدخلات runtime | مستثنيان |
| `tests/` وملفات `.bak` | أدوات اختبار ونسخ احتياطية | مستثناة |
| `.git/` و`.manus-logs/` و`.webdev/` | بيانات محلية وبيانات تشغيل | مستثناة |
| `scripts/*.py` | أدوات توليد توثيق محلية، مع إبقاء `scripts/load-env.js` | مستثناة |
| `logo-new.png` | غير مستخدم وغير مشار إليه | حُذف بأمان |
| `react-logo*.png` و`partial-react-logo.png` | أصول صغيرة متبقية من القالب | حجمها منخفض ولا تفسر توقف الرفع عند 1%؛ يمكن تنظيفها لاحقاً بعد اختبار شامل إن لم توجد مراجع |

## التوصية النهائية

أصبح المشروع أخف بصورة واضحة، وأفضل إجراء تالٍ هو بدء رفع سحابي جديد من نسخة المشروع الحالية بعد حفظ checkpoint، مع عدم إعادة إضافة مجلدات `docs` أو `presentation-assets` أو ملفات الاختبار إلى ملف الرفع. إذا استمر التوقف عند 1% بعد هذا الخفض، فالأرجح أن المشكلة خارج حجم الكود والأصول، في طبقة جلسة الرفع أو التحضير لدى منصة البناء. عندها ينبغي إعادة المحاولة من جلسة رفع جديدة، وتجربة شبكة أو متصفح مختلف، ومقارنة سجل الرفع؛ لا توجد في نتائج هذا التدقيق أصول كبيرة غير مستثناة تفسر وحدها التوقف.

هذا التقرير لا يوصي بحذف `react-logo*.png` تلقائياً قبل فحص كل المراجع، لأن فائدتها الحجمية محدودة مقارنة بمخاطر إزالة أصل قد يستخدمه مسار احتياطي أو شاشة تطوير.
