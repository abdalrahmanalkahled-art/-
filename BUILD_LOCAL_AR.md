# بناء مدير تسويق مدار محلياً

هذه الحزمة تحتوي على **مصدر التطبيق فقط**. لا تحتوي على `node_modules` أو مخرجات البناء أو الملفات المؤقتة؛ لذلك يجب تثبيت الاعتماديات قبل التشغيل.

> للبناء من هاتف Android فقط عبر GitHub وExpo EAS، اقرأ ملف [`BUILD_FROM_PHONE_AR.md`](./BUILD_FROM_PHONE_AR.md). يتضمن المشروع أيضاً إعداد `eas.json` وملف إجراء GitHub جاهزاً لإنشاء APK.

## المتطلبات

| المتطلب | الإصدار أو الغرض |
|---|---|
| Node.js | الإصدار 22 أو أحدث متوافق |
| pnpm | الإصدار 9.12.0 |
| Android Studio | لتثبيت Android SDK والمحاكي أو بناء APK محلياً |
| Java JDK | الإصدار 17 |
| هاتف Android أو محاكي | لاختبار التطبيق |

## تجهيز المشروع

افتح الطرفية داخل مجلد المشروع، ثم نفّذ الأوامر التالية:

```bash
npm install -g pnpm@9.12.0
pnpm install --frozen-lockfile
pnpm run check
```

لا توجد مفاتيح API مطلوبة لتشغيل الوظائف المحلية الأساسية في التطبيق.

## تشغيل التطبيق أثناء التطوير

لتشغيله على هاتف Android موصول عبر USB أو على محاكي Android:

```bash
pnpm android
```

سيُنشئ Expo ملفات Android الأصلية عند الحاجة، ثم يثبت نسخة تطوير على الجهاز أو المحاكي. فعّل «خيارات المطوّر» و«تصحيح USB» عند استخدام هاتف فعلي.

## إنشاء APK محلياً

بعد نجاح تشغيل Android، يمكن إنشاء نسخة Debug قابلة للتثبيت من Android Studio، أو عبر Gradle الناتج بعد تشغيل أمر Android السابق. المسار المتوقع لنسخة Debug هو:

```text
android/app/build/outputs/apk/debug/app-debug.apk
```

لإنشاء نسخة Release موقعة للنشر، افتح مجلد `android` في Android Studio، ثم استخدم:

```text
Build → Generate Signed Bundle / APK → APK
```

أنشئ ملف توقيع خاصاً بك واحفظه في مكان آمن. لا تشارك مفتاح التوقيع أو كلمات مروره داخل المستودع أو عبر المحادثات.

## التحقق قبل البناء

نفّذ الفحوصات التالية قبل إنشاء نسخة جديدة:

```bash
pnpm run check
pnpm test -- --run
pnpm run lint
```

## ملاحظات مهمة

يتضمن التطبيق ميزات أصلية مثل الكاميرا واختيار الصور والفيديو والإشعارات واختيار ملفات النسخ الاحتياطي؛ لذا ينبغي الاختبار على جهاز أو محاكي Android، وليس المتصفح وحده. ستظهر طلبات الأذونات عند استخدام الميزات المرتبطة بها لأول مرة.
