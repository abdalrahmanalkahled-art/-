# تدقيق منطق التفاعل والواجهات — مساعد التسويق الميداني

**النطاق:** تدقيق ثابت لتدفقات النوافذ والرجوع والتنقل والأزرار والحفظ المحلي، مع افتراض أن التطبيق مخصص حالياً لاستخدام **هاتف واحد**. لم تُجرَ أي تعديلات وظيفية ضمن هذا التدقيق.

## الملخص التنفيذي

التطبيق يملك أساساً منطقياً جيداً لتطبيق ميداني محلي: البيانات محفوظة على الجهاز، العمليات الطويلة تعرض تقدماً حجبياً، والنسخ الاحتياطي يتضمن تحققاً من البنية والمساحة وتدفقاً للملفات الكبيرة. كما أن مسارات المحلات والفعاليات تستخدم ترتيباً صريحاً لإغلاق معظم الطبقات عند الرجوع.

> أكبر فرصة تحسين ليست إضافة ميزة جديدة، بل **توحيد إدارة الطبقات المفتوحة**. النوافذ موزعة بين حالات محلية كثيرة، بينما معالج الرجوع المركزي موجود في عدد محدود من الشاشات فقط. هذا قد يسبب تجربة رجوع غير متسقة عندما تتداخل نافذة فرعية مع نموذج أو شاشة تفاصيل.

| مؤشر تدقيق | الملاحظة |
|---|---:|
| ملفات تحتوي نوافذ أو أغلفة نوافذ | 73 ملفاً |
| ملفات تستخدم معالج الرجوع المركزي | 6 ملفات |
| ملفات تستدعي `router.back()` مباشرة | 16 ملفاً |
| ملفات تستدعي `Alert.alert` مباشرة | 36 ملفاً |
| أكبر شاشة تفاعلية | `app/(tabs)/surveys.tsx`، 2245 سطراً |

## خريطة المنطق الحالي

يتبع التدفق المعتاد في الوحدات الرئيسية المسار التالي: **ضغط المستخدم ← حالة محلية ← نافذة أو صفحة تفاصيل ← تأكيد عند الإجراء الحساس ← حفظ محلي ← نجاح أو تقدم عملية**. تستخدم النماذج الكبيرة غلاف `FloatingFormModal`، وتستخدم التأكيدات `ConfirmDialog`، بينما تستخدم إجراءات الضغط المطوّل `CardActionModal`.

يوجد في الجذر حارس دخول وشاشة تحميل وحد خطأ ومزود تنبيه عام وطبقة تقدم للعمليات. زر رجوع أندرويد في الجذر يسمح للتنقل الطبيعي بالعمل، بينما `useOverlayBackHandler` يعترض الرجوع داخل الشاشات التي تستخدمه ويغلق طبقة محلية قبل مغادرة الشاشة (`lib/use-overlay-back-handler.ts`).

| التدفق | التقييم | الدليل المنطقي |
|---|---|---|
| الفعاليات | جيد نسبياً | يغلق اختيار الهدف ثم التاريخ ثم الوسائط ثم التأكيد ثم الإجراءات ثم النموذج ثم التفاصيل. |
| المحلات | جيد نسبياً | يغلق رسالة الخطأ والتأكيدات ومدير الفئات وإجراءات البطاقة والوسائط والنموذج والتفاصيل بترتيب صريح. |
| الاستبيانات | قوي لكن عالي التعقيد | توجد خريطة رجوع لـ10 طبقات، لكن توجد طبقات أخرى خارج هذه الخريطة. |
| تقارير زيارة السوق | يحتاج توحيداً | يفتح إعدادات ووسوماً ومنتجات ووسوم منتجات كنوافذ محلية متداخلة بلا معالج رجوع مركزي. |
| عمليات التصدير والاستعادة | جيد | طبقة التقدم غير قابلة للإغلاق عرضاً أثناء العملية وتوضح المرحلة وعدد الوسائط. |

## نقاط القوة

**الرجوع المتسلسل موجود فعلاً** في تدفقات مهمة. دالة `closeTopOverlay` تغلق أول طبقة فقط، فلا تقفز مباشرة من نافذة فرعية إلى شاشة سابقة. وهذا يطابق سلوك الهاتف المتوقع: رجوع واحد لكل طبقة.

**الإجراءات الحساسة محمية غالباً.** الحذف والاستعادة وعمليات مماثلة تستخدم `ConfirmDialog` مع حالة `isSubmitting` تمنع التكرار وتمنع الإغلاق الخارجي أثناء التنفيذ. كما أن طبقة تقدم العمليات الطويلة لا تقدم زر إلغاء وهمياً.

**منطق الهاتف الواحد مناسب من حيث المبدأ.** السجلات في `AsyncStorage` والوسائط في تخزين الجهاز، والاستعادة تتحقق من بنية النسخة والمسارات الآمنة والمساحة الحرة، وتستخدم تدفقاً متقطعاً للنسخ الكبيرة. كما يوجد تراجع لكتابة البيانات إذا فشلت عملية `multiSet`.

## المخاطر والفجوات

| الأولوية | الملاحظة | الأثر المحتمل | التوصية |
|---|---|---|---|
| عالية | لا توجد طبقة موحدة لجميع النوافذ | الرجوع قد يغلق النموذج الأم قبل قائمة فرعية أو تأكيد ظاهر في بعض التدفقات. | اعتماد سجل طبقات موحد لكل شاشة مركبة، وترتيب الإغلاق من الأعلى إلى الأسفل. |
| عالية | الاستبيانات تجمع طبقات أكثر من خريطة الرجوع | إجراءات القالب والنتيجة وتصدير النتائج وتفاصيل الدورة وحذف الوسائط واستيراد القالب لا تدخل كلها في `getSurveyOverlayToClose`. | توسيع الخريطة أو استبدالها بمدير طبقات موحد. |
| متوسطة | قوائم المنطقة/التصنيف داخل النماذج ليست دائماً ضمن الرجوع | قد يغلق زر الرجوع النموذج بدلاً من القائمة المنسدلة المفتوحة. | اعتبار كل منتقي أو قائمة فرعية طبقة مستقلة بأولوية أعلى من النموذج. |
| متوسطة | `CardActionModal` لا يغلق نفسه عند تنفيذ الإجراء | تعتمد سلامة الانتقال على أن كل مستدعٍ يغلقه يدوياً قبل فتح الطبقة التالية. | جعل الإغلاق جزءاً من عقد الإجراء أو تقديم دالة موحدة: `closeThen(action)`. |
| متوسطة | إغلاق النموذج الخارجي لا يميّز دائماً بين مسودة نظيفة وغير محفوظة | يمكن فقدان إدخال في نموذج طويل عند الضغط خارج حدوده. | إضافة حارس مسودة يعرض «حفظ المسودة / تجاهل / متابعة التحرير» عند وجود تغييرات. |
| متوسطة | التنبيه العام يحتفظ بحالة واحدة فقط | تنبيهان متقاربان قد يستبدل أحدهما الآخر؛ كما يسمح النقر الخارجي بالإغلاق. | إضافة طابور تنبيهات، واستخدام تأكيدات صريحة دائماً للأفعال المدمرة. |
| متوسطة | ملفات التفاعل كبيرة جداً | صعوبة مراجعة كل انتقال، وارتفاع احتمال نسيان طبقة عند إضافة ميزة. | فصل الحالات إلى hooks: نموذج، تفاصيل، إجراءات، استيراد/تصدير، وفلاتر. |
| منخفضة | طابور `offline-sync` محلي فقط ولا يظهر له مستهلك في الطبقة المقروءة | يضيف بيانات وسجلات بلا منفعة مباشرة في وضع هاتف واحد. | إما إظهاره كسجل نشاط محلي أو تقليل ما يسجل فيه حتى لا ينمو بلا حاجة. |

## ملاحظات خاصة بالهاتف الواحد

لا أنصح الآن بإضافة خادم أو مزامنة متعددة الأجهزة؛ سيزيد ذلك احتمال التعارض من دون أن يخدم هدفك الحالي. البنية المحلية الحالية منطقية، لكن يلزم التعامل مع الهاتف نفسه كأنه **مصدر البيانات الوحيد**.

المخاطر العملية هنا هي فقدان الهاتف أو حذف التطبيق أو إعادة ضبطه أو امتلاء التخزين. النسخ الاحتياطي الحالي قوي نسبياً، لكنه لا يجعل الاستعادة ذرية بالكامل: تُستعاد الوسائط قبل تحديث البيانات؛ فإذا فشلت كتابة البيانات لاحقاً قد تبقى ملفات وسيطة أو غير مرتبطة. هذا لا يعني فساداً مؤكداً، لكنه سبب جيد للاستمرار في توفير تنظيف الملفات المهملة وسجل استعادة واضح.

تُحفظ الجلسة والبيانات العامة في `AsyncStorage`، وتوجد كلمة مرور افتراضية محلية. لهاتف شخصي مقفل هذا مناسب للبدء، لكنه ليس حداً أمنياً كافياً إذا وصل شخص آخر إلى الجهاز المفتوح. أنصح بإلزام تغيير كلمة المرور الافتراضية وإضافة قفل اختياري ببصمة أو رمز عند العودة للتطبيق بعد مدة خمول.

## خطة تحسين مرتبة

### 1. مدير طبقات موحد — الأولوية الأولى

أنشئ مفهوماً واحداً لـ`OverlayStack` داخل كل شاشة كثيفة. تسجل الشاشة حالة الطبقات بترتيب صريح: قائمة فرعية، منتقي، تأكيد، إجراءات، نموذج، تفاصيل. يجب أن يقوم زر الرجوع أو الإيماءة أو الضغط الخارجي بطلب إغلاق **آخر طبقة فقط**. هذا يعالج الاستبيانات والتقارير والمخازن والمحلات بطريقة واحدة.

### 2. حارس المسودات للنماذج — الأولوية الثانية

احتفظ بالمسودة الحالية في النموذج، لكن لا تغلق النموذج المتغير مباشرة. عند الرجوع أو الضغط الخارجي، أظهر تأكيداً موجزاً. استخدم الحفظ التلقائي الحالي للمحلات كنمط مرجعي، ثم طبقه على الاستبيان والفعالية والمنتجات والإعدادات الطويلة.

### 3. توحيد الانتقالات الحساسة — الأولوية الثالثة

اجعل أي انتقال من «إجراءات» إلى «تعديل» أو «حذف» يمر عبر دالة مشتركة تغلق طبقة الإجراءات أولاً. واجعل كل عملية حفظ أو حذف تستخدم حالة تحميل تمنع النقر المكرر، مع نجاح واحد فقط بعد اكتمال التخزين.

### 4. تقسيم الشاشات الكبيرة واختبار التدفقات — الأولوية الرابعة

ابدأ بالاستبيانات، ثم اللوحات والستاندات، ثم المحلات والفعاليات. لا يلزم تغيير الواجهة؛ الهدف فصل منطق الحالات. أضف اختبارات لكل ترتيب رجوع، لاختبار المكون المركزي فقط.

## سيناريوهات اختبار يدوية مقترحة على الهاتف

| السيناريو | النتيجة الصحيحة |
|---|---|
| فعالية: اختيار هدف ← منتقي تاريخ ← وسائط ← رجوع متكرر | يغلق آخر عنصر مفتوح في كل مرة، ثم النموذج، ثم يعود للصفحة. |
| محل: نموذج ← قائمة منطقة أو تصنيف ← إضافة منطقة ← رجوع | يغلق إضافة المنطقة ثم القائمة ثم النموذج، ولا يخرج من الشاشة مباشرة. |
| استبيان: إجراءات قالب ← تأكيد إغلاق/حذف ← رجوع | يغلق التأكيد ثم الإجراءات ثم يبقى في قائمة القوالب. |
| تقارير: إعدادات ← وسوم ← منتجات مؤشرات ← وسوم المنتجات ← رجوع | يعود طبقة واحدة في كل ضغط ولا يغلق إعدادات التقارير دفعة واحدة. |
| عملية استعادة كبيرة | لا يسمح بضغطات مكررة ولا بخروج وهمي أثناء الكتابة، ويظهر نجاح أو فشل واضح. |
| إضافة سريعة متكررة أو ضغط مزدوج على حفظ | لا ينشئ عنصراً مكرراً ولا رسالة نجاح مكررة. |

## الخلاصة

التطبيق مناسب منطقياً لاستخدام ميداني محلي على هاتف واحد، وأقوى عناصره هي الحفظ المحلي والنسخ الاحتياطي وتقدم العمليات والتأكيدات المخصصة. التحسين الأعلى أثراً هو **تحويل إدارة النوافذ من حالات متفرقة إلى نظام طبقات موحد**، ثم حماية المسودات. هذان التحسينان سيزيدان موثوقية الرجوع ويقللان فقدان الإدخال، من دون الحاجة إلى خادم أو إعادة تصميم واسعة.
