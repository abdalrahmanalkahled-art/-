# مراجعة برمجية مركزة — الأكواد المكررة والزائدة

**نطاق المراجعة:** تحليل ساكن للمشروع، ملف الاعتماديات، فحص lint، وإحصاءات حجم الملفات. لم يُعدّل أي كود وظيفي ضمن هذه المراجعة.

## الحكم المختصر

لا توجد مؤشرات على «كود عبثي» بمعنى كود ضار أو غير مفهوم عمداً. لكن يوجد **تراكم تجريبي وتكرار فعلي** من مراحل التطوير السابقة: استيرادات وحالات غير مستخدمة، نوافذ تأكيد قديمة بجانب نافذة موحدة جديدة، منطق اختيار وسائط مكرر، وشاشات كبيرة جداً تجمع واجهة المستخدم والتخزين والتصدير والنوافذ في ملف واحد.

> أهم ملاحظة: المشروع يمر TypeScript والاختبارات، لكنه يفشل حالياً في lint بسبب **مشكلتين فعليتين و188 تحذيراً**. هذا لا يمنع التشغيل دائماً، لكنه يرفع مخاطر الانحدار عند أي تعديل لاحق.

## ما وجدته مؤكداً

| الأولوية | الملاحظة | الدليل | الأثر المقترح |
|---|---|---|---|
| حرجة للصيانة | ملف الاستبيانات ضخم جداً | `app/(tabs)/surveys.tsx` ≈ 2,153 سطراً | تقسيمه إلى hooks ونوافذ وخدمات تصدير؛ هو أكبر مصدر محتمل للأخطاء المستقبلية. |
| عالية | تكرار بنية الشاشات | المحلات ≈ 1,013، الفعاليات ≈ 926، اللوحات ≈ 1,617 سطراً | استخراج `FormModal` و`MediaPicker` و`DeleteFlow` وخدمات تحميل/حفظ مشتركة. |
| عالية | أمان الأنواع ضعيف | 553 تطابقاً لكلمة `any` في طبقات التطبيق | استبدالها تدريجياً بأنواع مجال الأعمال، خصوصاً الوسائط والاستبيانات والتخزين. |
| عالية | lint غير نظيف | 2 أخطاء و188 تحذيراً | لا تنشر تغييرات كبيرة قبل إصلاح الأخطاء ثم التحذيرات ذات الصلة بالـ hooks. |
| متوسطة | استيرادات وحالات مهجورة | `events.tsx`, `stores.tsx`, `surveys.tsx`, `more.tsx` تحوي متغيرات واستيرادات لا تستعمل | حذفها يقلل التشويش وحجم الحزمة الذهنية للمطور. |
| متوسطة | تكرار وسائل التأكيد والإشعار | بقيت 4 ملفات تحوي تأكيدات مخصصة بجانب `ConfirmDialog` | ترحيل البقية إلى `ConfirmDialog` ثم حذف CSS والحالات المتكررة. |
| متوسطة | 100 `console.log` | فحص الكود أعطى 100 موضع | استبدالها بواجهة logging واحدة قابلة للتعطيل في الإنتاج. |
| متوسطة | 116 `Alert.alert` | موزعة بين الشاشات والمكونات | الإبقاء على أخطاء النظام فقط؛ توحيد النجاح والفشل في toast/modal. |

## التفاصيل المهمة

### 1. نوافذ التأكيد ما زالت في مرحلة انتقالية

تم ربط `ConfirmDialog` حديثاً بالمحلات والفعاليات والاستبيانات، وهذا صحيح. لكن توجد تأكيدات مخصصة أخرى في لوحة التحكم، الصور، وبعض الوحدات. لا أنصح بحذف أي نافذة قديمة فوراً؛ الأفضل إجراء ترحيل شاشة بعد شاشة ثم حذف الأنماط والمتغيرات التي أصبحت بلا مرجع.

### 2. منطق الوسائط مكرر

يتكرر استدعاء المكتبة والكاميرا ومعالجة الأخطاء في المحلات والفعاليات والاستبيانات، وظهر **18 موضعاً** لاستدعاءات `launchImageLibrary` و`launchCamera`. يوجد بالفعل غلاف `media-picker.ts`، ولذلك أفضل تحسين هو توحيد دوال الشاشة حول هذا الغلاف بدلاً من نسخ التنبيهات وحالات الوسائط.

### 3. تحذيرات hooks ليست تجميلية فقط

هناك تحذيرات dependencies في `stores.tsx` و`surveys.tsx` و`store-detail.tsx` ومكونات أخرى. بعضها يمكن أن يسبب دوالاً قديمة أو إعادة تحميل غير متوقعة، خصوصاً دوال `showError` المعرفة داخل المكون ثم المستعملة في `useCallback`. هذا يحتاج معالجة قبل تحسين الأداء، لأن تجميد الواجهة أو قراءة حالة قديمة على الهاتف أصعب في التتبع من تحذير lint.

### 4. عناصر اختبار وتبعيات متروكة

لا توجد `expo-document-picker` أو `react-native-image-picker` ضمن اعتماديات التطبيق الفعلية حالياً، لكن اختبار التصدير لا يزال يحتوي mock لمكتبة `expo-document-picker` التي أزيل استخدامها. هذا **كود اختبار زائد مؤكد** ويمكن حذفه بأمان بعد تشغيل الاختبارات. كما توجد `axios` و`mysql2` و`zustand` في الاعتماديات؛ استعمالها موجود على مستوى المشروع، لذلك لا أصفها بالزائدة قبل مراجعة سبب وجود كل منها.

### 5. أخطاء lint الحالية

الخطآن الوحيدان من قاعدة `react/no-unescaped-entities` في صفحة المحلات، إضافة إلى تحذيرات متعددة. بعد إصلاحهما يجب ضبط CI على فشل lint عند التحذيرات الجديدة، مع معالجة القديم على دفعات. إضافة `"type": "module"` إلى `package.json` لا أوصي بها مباشرة فقط لإسكات تحذير ESLint؛ لأنها قد تؤثر في ملفات إعدادات CommonJS، ويجب التخطيط لها منفصلة.

## خطة تنظيف آمنة

| المرحلة | العمل | معيار النجاح |
|---|---|---|
| 1 | إزالة الاستيرادات والمتغيرات غير المستخدمة وتحديث mock القديم | lint بلا أخطاء، وتحذيرات أقل بوضوح، والاختبارات ناجحة. |
| 2 | ترحيل جميع تأكيدات الحذف/الخروج إلى `ConfirmDialog` | لا تبقى نافذة تأكيد مخصصة في الشاشات الرئيسية. |
| 3 | استخراج منطق الوسائط والتصدير من المحلات والفعاليات والاستبيانات | دوال اختيار الصورة والتصدير في خدمات مشتركة، بلا نسخ. |
| 4 | تفكيك `surveys.tsx` أولاً ثم `signage-module.tsx` | كل ملف شاشة أقل من 600 سطر تقريباً. |
| 5 | تخفيض `any` في البيانات الحساسة | أنواع واضحة لنتائج الاستبيانات والوسائط وسجلات التخزين. |

## رأيي الفني

المشروع ليس فوضوياً، لكنه وصل إلى مرحلة يحتاج فيها **تنظيفاً منظماً قبل إضافة ميزات كبيرة**. الأولوية ليست تغيير شكل الواجهة، بل تقليل التكرار في `surveys` و`stores` و`events`، وإغلاق تحذيرات hooks، وحذف البقايا التجريبية. بعد دورة تنظيف واحدة، ستكون سرعة إضافة CRM والتقارير أكبر بكثير وأقل مخاطرة.
