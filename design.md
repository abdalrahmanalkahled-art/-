# تصميم واجهات «مدير تسويق مدار»

## مبادئ التصميم

يُصمم التطبيق للاستخدام العمودي بنسبة **9:16** وباليد الواحدة، مع واجهة عربية RTL واضحة وأولويات عملية لمدير التسويق الميداني. تتبع الشاشات نمط iOS المألوف: شريط تنقل سفلي قصير، عناوين كبيرة، بطاقات هادئة بزوايا مستديرة، ومساحات لمس لا تقل عن 44 نقطة. تُحفظ الإجراءات الجوهرية في أسفل الشاشة أو في زر إجراء رئيسي يسهل بلوغه بالإبهام.

## قائمة الشاشات

| الشاشة | المحتوى والوظيفة الأساسية |
|---|---|
| الدخول | حقل تعريف المستخدم أو وسيلة الدخول المتاحة، ورسائل خطأ/تحميل واضحة قبل الدخول إلى مساحة العمل. |
| لوحة التحكم | ملخص للأهداف والحملات والمهام القريبة، مع مؤشرات سريعة وروابط إلى الإجراءات اليومية. |
| الحملات | قائمة الحملات، حالتها ومؤشر تقدمها، ثم صفحة تفصيل تتضمن ملخص الحملة وأنشطتها ونتائجها. |
| الزيارات | قائمة الزيارات الميدانية مع الحالة والتاريخ، وإضافة/توثيق زيارة باستخدام الملاحظات والصور عند توافر الإذن. |
| الفعاليات | جدول أو قائمة الفعاليات مع تفاصيل الموعد والموقع والمحتوى المطلوب وتوثيق التنفيذ. |
| التحليلات | بطاقات مؤشرات ورسوم/ملخصات قابلة للقراءة على شاشة الهاتف، مع رابط التحليلات الخارجية عند وجوده. |
| الإعدادات | تخصيص الواجهة، التنبيهات، الحساب، ومعلومات التطبيق. |

## التدفقات الرئيسة

| التدفق | المسار |
|---|---|
| متابعة العمل اليومي | فتح التطبيق ← لوحة التحكم ← اختيار بطاقة مهمة/حملة ← مراجعة التفاصيل ← تحديث الحالة أو إضافة نشاط. |
| توثيق زيارة | الزيارات ← إضافة أو فتح زيارة ← إدخال الملاحظات ← اختيار صورة/التقاطها عند الحاجة ← حفظ ← عرض إشعار نجاح. |
| إدارة فعالية | الفعاليات ← اختيار فعالية ← مراجعة التفاصيل ← تحديث حالة التنفيذ أو المحتوى ← الرجوع إلى القائمة. |
| الاطلاع على النتائج | لوحة التحكم أو التحليلات ← تحديد الحملة/الفترة ← قراءة المؤشرات ← فتح التحليل الخارجي إذا كان الرابط مفعلاً. |

## الألوان والهوية

تستخدم الهوية درجة كحلية داكنة `#111827` للثقة والقيادة، وأزرقاً مدارياً `#0E7490` للإجراءات الرئيسية، ولمسة تركوازية `#14B8A6` لحالات الإنجاز. الخلفيات فاتحة ودافئة `#F8FAFC`، والأسطح بيضاء `#FFFFFF`، مع نص رئيسي `#0F172A` وحدود `#E2E8F0`. للأوضاع المهمة تُستخدم ألوان دلالية واضحة: نجاح `#16A34A`، تنبيه `#D97706`، وخطأ `#DC2626`.

## معايير التفاعل والوصول

تعرض جميع الإجراءات حالات ضغط خفيفة، ورسائل نجاح أو فشل مفهومة، وحالات تحميل وفراغ صادقة بدلاً من أرقام تجريبية. تظل العناوين والأزرار قابلة للقراءة في الوضع الداكن، وتُستخدم المناطق الآمنة في كل شاشة لمنع تداخل المحتوى مع النتوء وشريط الصفحة الرئيسة.


## مواصفات إعادة تصميم الشاشة الرئيسية

### الهدف
إعادة تقديم الشاشة الرئيسية كلوحة قيادة يومية مختصرة لمسؤول التسويق الميداني، مع الحفاظ على إعدادات `DashboardSettings` الحالية الخاصة بإظهار الأقسام، ترتيبها، إظهار المخططات، إجراءات البدء، ومنتج مخطط التواجد.

### مبادئ التصميم
يعتمد التصميم على وضوح الأولويات، بطاقات قابلة للمس بحد أدنى 44×44، شبكة مسافات مبنية على 8 نقاط، تباين واضح، ونمط موحد مع بطاقات ونوافذ تطبيق مدار. الشاشة RTL نصياً مع الحفاظ على ترتيب عناصر التحكم المتفق عليه في التطبيق.

### الهيكل المقترح
1. رأس يومي مضغوط: التحية، التاريخ، الحساب، والإشعارات.
2. بطاقة «التقرير اليومي» كإجراء رئيسي واضح.
3. شريط مؤشرات العمل: فعاليات، محلات، استبيانات، ومتوسط التواجد.
4. «أولوية اليوم» والتنبيهات، مع حالات فارغة هادئة وواضحة.
5. المخططات المفعّلة فقط، مع الحفاظ على اختيار المنتج ومفاتيح المخططات.
6. إجراءات «ابدأ مهمة» المفعّلة فقط.
7. أولوية الخطة والفعاليات الأخيرة وتغطية المناطق وفق ترتيب التخصيص.

### الحفاظ على التخصيص
لا تتغير قيم `DASHBOARD_SECTION_IDS` أو `DASHBOARD_CHART_IDS` أو `DASHBOARD_QUICK_ACTION_IDS`. يستمر `enabledSections` في التحكم بالترتيب والإظهار، ويستمر `enabledCharts` في التحكم بالمخططات، ويستمر `enabledQuickActions` في التحكم بالإجراءات، ويظل `presenceProductId` مرتبطاً بمخطط التواجد. أي قسم غير مفعّل لن يُرسم، وأي إعداد قديم غير معروف سيُطبّع عبر `normalizeDashboardSettings`.

### حالات الشاشة
تشمل الشاشة حالة تحميل هيكلية، حالة بيانات فارغة، حالة وجود تنبيهات، حالة عدم وجود أولوية ميدانية، وحالة عدم اختيار منتج لمخطط التواجد. لا تستخدم الشاشة أرقاماً تجريبية أو محتوى ثابتاً يوحي ببيانات غير موجودة.

### معيار النجاح
يجب أن تبدو الشاشة أكثر هدوءاً وترابطاً، وأن تبقى كل بطاقات وأقسام التخصيص قابلة للإظهار والإخفاء وإعادة الترتيب دون فقدان الإعدادات أو تغيير البيانات المحفوظة.
