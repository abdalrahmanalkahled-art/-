# بناء مدير تسويق مدار من الهاتف فقط

هذا الدليل مخصص لمن يملك هاتف Android فقط ويريد إنشاء ملف تثبيت للتطبيق من مصدر المشروع. يتضمن المشروع إعداداً جاهزاً لبناء **APK** قابل للتثبيت، عبر GitHub وExpo EAS. لا تحتاج إلى تثبيت Android Studio أو تشغيل بيئة بناء ثقيلة على الهاتف.

## ما تحتاج إليه

| المتطلب | الغرض |
|---|---|
| حساب GitHub | حفظ المصدر وتشغيل أمر البناء من المتصفح. |
| حساب Expo لديه حصة بناء | تنفيذ إنشاء ملف Android. |
| تطبيق لفك الضغط | مثل مدير الملفات أو ZArchiver. |
| متصفح الهاتف | لإدارة GitHub وExpo وتنزيل APK. |

## الخطوة الأولى: رفع المشروع إلى GitHub

فك ضغط ملف المشروع، ثم افتح [GitHub](https://github.com) من المتصفح وأنشئ مستودعاً جديداً من نوع **Private**. ارفع محتويات مجلد المشروع نفسه، وليس المجلد الأب فقط. يجب أن تظهر ملفات مثل `package.json` و`app.config.ts` و`eas.json` في جذر المستودع.

> لا ترفع مجلد `node_modules` أو ملفات البناء المؤقتة. الحزمة المرسلة لك لا تحتويها أصلاً.

## الخطوة الثانية: إعداد حساب Expo الآخر

افتح [Expo](https://expo.dev) بالحساب الذي لديه حصة بناء. من إعدادات الحساب أنشئ **Personal Access Token** باسم واضح، مثل `github-android-build`، ثم انسخه فوراً واحفظه مؤقتاً في مكان آمن. لا ترسله في المحادثة ولا تضعه داخل ملفات المشروع.

بعد ذلك افتح مستودع GitHub، ثم اذهب إلى:

```text
Settings → Secrets and variables → Actions → New repository secret
```

أنشئ سراً بالاسم التالي تماماً:

```text
EXPO_TOKEN
```

وألصق فيه رمز Expo الذي أنشأته.

## الخطوة الثالثة: بدء بناء APK

من مستودع GitHub افتح تبويب:

```text
Actions → بناء APK لأندرويد → Run workflow
```

اختر `preview` ثم اضغط **Run workflow**. هذا الملف الشخصي ينشئ APK يمكن تثبيته مباشرة على الهاتف. انتظر حتى تنتهي المهمة، ثم افتح سجل الخطوة الأخيرة؛ سيظهر رابط بناء Expo. افتح الرابط ونزّل ملف APK.

## تثبيت APK

بعد تنزيل الملف، افتحه من تطبيق الملفات. إذا ظهر تنبيه، فعّل خيار السماح للتطبيق الذي تستخدمه للتثبيت من مصادر غير معروفة، ثم تابع التثبيت. لا تمنح هذا الإذن لتطبيقات غير موثوقة.

## متى أستخدم production؟

اختر `production` فقط عندما تريد رفع التطبيق إلى Google Play؛ فهو ينشئ **AAB** وليس APK. للاختبار أو التثبيت على هاتفك اختر دائماً `preview`.

## حل المشكلات الشائعة

| المشكلة | الحل |
|---|---|
| `EXPO_TOKEN` غير موجود | تأكد من إنشاء سر GitHub بالاسم نفسه، ومن أن الرمز صادر من حساب Expo الصحيح. |
| تجاوزت حصة Expo | استخدم حساب Expo آخر لديه حصة متاحة أو انتظر تجدد الحصة. |
| فشل تثبيت الاعتمادات | تأكد من رفع `pnpm-lock.yaml` و`package.json` كما هما، ولا تعدلهما من الهاتف. |
| ظهر بناء AAB بدلاً من APK | أعد تشغيل المهمة واختر `preview`. |
| لا يظهر رابط التنزيل | افتح سجل خطوة «ربط المشروع وبناء Android» وابحث عن رابط Expo الأخير. |

## تنبيه أمني

رمز `EXPO_TOKEN` يمنح صلاحية تنفيذ بناء باسم حساب Expo؛ احفظه داخل أسرار GitHub فقط. عند الانتهاء من المشروع أو عند الشك في انكشاف الرمز، ألغِه من حساب Expo وأنشئ واحداً جديداً.
