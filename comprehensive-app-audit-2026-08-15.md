# تقرير تدقيق مستقل — مدير تسويق مدار

**تاريخ التدقيق:** 15 آب 2026  
**منهج التدقيق:** مراجعة البنية والملفات الرئيسية، فحص TypeScript والاختبارات والاعتماديات، مراجعة تدفقات التصدير والمظهر والرجوع، ومعاينة مرئية للوحة التحكم وصفحة المحلات. هذا التقرير يفرق بين ما هو **منفذ ومتحقق** وما هو **مضاف كملف غير مربوط بالواجهة أو بالتدفق الفعلي**.

## الخلاصة التنفيذية

التطبيق يمتلك قاعدة قوية لتطبيق إدارة تسويق ميداني محلي: واجهة عربية جيدة، مجالات أعمال واسعة، استعمال صحيح إجمالاً لـ Expo Router وAsyncStorage وReact Query، وفحص TypeScript نظيف مع **100 اختبار ناجح واختبار واحد متخطى**. لكن حكمي المهني الصريح هو أنه **ليس جاهزاً بعد لتوسيع سريع في المزايا أو للاعتماد التشغيلي الكامل**؛ لأن بعض المشاكل التي أبلغ عنها المستخدم ما زالت ظاهرة في مسارات الكود، وعدة تحسينات أُنشئت كملفات مستقلة دون ربطها فعلياً بالتطبيق.

> **التقييم العام الحالي: 2.8 / 5.**
>
> المنتج غني وظيفياً ومبشر، لكن يحتاج دورة تثبيت ودمج واختبار ميداني قبل إضافة CRM أو تقارير أكثر تعقيداً.

| المنظور | التقييم | الحكم المختصر |
|---|---:|---|
| البنية والأنواع | 3.5 / 5 | طبقات موجودة وفحص TypeScript نظيف، لكن الملفات العملاقة تخفف القابلية للصيانة. |
| الاستقرار الأصلي على الهاتف | 2.5 / 5 | توجد معالجة جيدة لبعض الأعطال، لكن النوافذ والكيبورد والرجوع غير موحدة في التطبيق كله. |
| تجربة المستخدم | 3 / 5 | العربية والحالات الفارغة والإجراءات السريعة جيدة؛ الاتساق واكتشاف الإعدادات بحاجة لتحسين. |
| التصدير والتقارير | 1.5 / 5 | مسار CSV الحالي ما زال يمرر أسماء ملفات غير معقمة، والحلول المنشأة غير موصولة به. |
| الاختبارات والإصدار | 3 / 5 | الاختبارات والاعتماديات المحلية سليمة، لكن لا توجد اختبارات تدفق أصلية أو E2E، وسجل النشر يبين حساسية قفل الحزم. |

## ما تم التحقق منه

تم تشغيل `pnpm install --frozen-lockfile --offline` بنجاح، ثم فحص TypeScript، ثم Vitest. النتيجة كانت **9 ملفات اختبار ناجحة، ملف واحد متخطى، 100 اختبار ناجحاً من 101**. كما أعاد `expo install --check` أن الاعتماديات متوافقة محلياً. هذه نقطة قوة مهمة لأنها تعني أن المشروع يمر من بوابة الأنواع والاختبارات الحالية.

لكن هذه النتيجة لا تثبت صحة التطبيق على الهاتف؛ فالاختبارات تركز أساساً على التحويلات والتحميل والتصدير الجزئي، وليس على فتح النوافذ، رجوع الإيماءات، لوحة المفاتيح، أو الكتابة الفعلية إلى نظام ملفات Android.

## منظور المبرمج

### البنية وتدفق البيانات

البنية الأساسية منطقية: `app/` للشاشات والتنقل، و`components/` للمكونات والوحدات، و`lib/` للتخزين والتصدير والسياقات والخدمات. جذر التطبيق يضم موفري React Query وtRPC والحالة الآمنة و`AppProvider` و`ThemeProvider`، وهي طبقات مناسبة لتطبيق Expo متوسط الحجم.[1]

لكن المشروع يضم **105 ملفات TypeScript/TSX ونحو 25,536 سطراً**، مع تركّز كبير للمسؤوليات في ملفات واحدة. أكبرها صفحة الاستبيانات بحجم **2,185 سطراً**، ثم وحدة اللوحات والستاندات **1,617 سطراً**، ثم التحليلات **1,010 سطراً**. هذا يرفع احتمال الانحدارات عند أي تعديل في النوافذ أو الحالة أو التصدير.[2]

| الملاحظة التقنية | الدليل | الأثر | الأولوية |
|---|---|---|---|
| ملفات كبيرة جداً | `surveys.tsx` 2,185 سطراً؛ `signage-module.tsx` 1,617 | صعوبة الاختبار وتكرار منطق النوافذ والحقول والرجوع | عالية |
| استعمال واسع لـ `any` | 559 تطابقاً في `app/components/hooks/lib` | يقلل حماية TypeScript عند التحويل والتخزين ووسائط الصور | عالية |
| 51 نافذة منبثقة | 12 ملفاً تحتوي نوافذ بلا `onRequestClose` على مستوى الملف | رجوع الإيماءات لا يكون موحداً في Android | حرجة |
| 15 استعمالاً لـ `KeyboardAvoidingView` مع `height` | منطق غير موحد بين الشاشات | خطر قفز الأزرار أو تغطيتها من لوحة المفاتيح | حرجة |

### جودة التنفيذ والاختبارات

الإيجابي أن المشروع يحتوي على منطق جيد قابل للاختبار في بعض المناطق مثل حالة التحميل، تحويل CSV، انتقاء الوسائط، وحالة الزر العائم. لكن حجم الاختبارات لا يساوي بالضرورة التغطية الفعلية؛ لا توجد اختبارات تفاعلية أصلية تؤكد أن المستخدم يستطيع فتح «إضافة محل»، الكتابة، إخفاء لوحة المفاتيح، الرجوع بالإيماءة، ثم حفظ السجل.

يوجد أيضاً فرق واضح بين **إضافة ملفات** و**تفعيل ميزة**. تم العثور على ملفات باسم `export-sanitizer.ts` و`reports-center-module.tsx` و`theme-mode.tsx` و`offline-sync.ts` و`audit-log.ts`، لكن البحث عن استيرادها داخل التطبيق أعطى **صفر مراجع** لكل منها. لذلك لا ينبغي اعتبار تعقيم التصدير، مركز التقارير، وضع المظهر الجديد، طابور العمل دون اتصال، أو سجل التدقيق ميزات مستخدمة حالياً؛ إنها مسودات أو مكونات غير مدمجة.[3]

### التصدير والإصدار

العطل الذي وصفه المستخدم صحيح تقنياً. دالة التصدير الحالية تبني URI مباشرة على صيغة `${documentDirectory}${filename}.csv`، بينما أسماء التقارير في المحلات والاستبيانات تستخدم `toLocaleDateString('ar-SA')` الذي قد ينتج `/` داخل الاسم.[4] لذلك قد يفسر Android الاسم كمسارات مجلدات متعددة وينتج `ENOENT`. ملف التعقيم موجود، لكنه غير مستورد في مسار التصدير، وبالتالي لا يحل المشكلة حالياً.

يوجد تعارض ثانٍ: `expo-document-picker` مستورد مع دالة `selectSavePath`، لكن هذه الدالة غير مستدعاة في تصدير CSV أو JSON. كذلك لا توجد أي إشارة إلى `expo-print`، وبالتالي معاينة أو تصدير PDF ليست ميزة منفذة حالياً.[4]

من ناحية الإصدار، `pnpm install --frozen-lockfile` نجح محلياً، وهو مؤشر جيد. لكن سجل النشر السابق أظهر فشلاً بسبب اختلاف مواصفات `package.json` و`pnpm-lock.yaml`. لذلك ينبغي اعتبار نجاح القفل محلياً خطوة مطمئنة، لا ضماناً أن كل نشر مستقبلي سيعمل ما لم يتم إنشاء نقطة حفظ بعد كل تغير في الاعتماديات والتحقق من النشر نفسه.

## منظور المستخدم

المعاينة البصرية للوحة التحكم جيدة مبدئياً. الواجهة عربية، التسلسل من اليمين إلى اليسار مقبول، وتظهر مؤشرات رئيسية وإجراءات سريعة وشريط تنقل بخمس وجهات. كما أن صفحة المحلات تعرض حالة فارغة مفهومة وزر «إضافة محل» واضحاً، وهو سلوك جيد للمستخدم الجديد.[5]

لكن التجربة ليست موحدة بعد. المستخدم يجد زر إضافة واضحاً في الحالة الفارغة وزراً عائماً في المكان نفسه؛ وجود مسارين للعمل مفيد لكنه يحتاج قاعدة ثابتة: الزر العائم للإضافة في كل القوائم، والحالة الفارغة تستخدمه مع نص توجيهي فقط أو زر واحد متسق. كما أن لوحة التحكم بقيم صفرية لا تشرح رحلة البداية: ما الإجراء الأول؟ هل أضيف محلاً أم فعالية أم مخزوناً؟

الأخطر في تجربة المستخدم هو أن «المزيد» يعرض إعدادات تبدو قابلة للنقر مثل إدارة المستخدمين والنسخ الاحتياطي وحول التطبيق، ولكن لا توجد لها معالجات `onPress` في الصفحة. هذا يصنع انطباعاً أن التطبيق لا يستجيب. صفحة المزيد نفسها تفتح الوحدات في `Modal` مع `KeyboardAvoidingView` بنمط `height`، ومن دون `onRequestClose` في النافذة الرئيسية، لذلك هي مثال مباشر على سبب بلاغات المستخدم عن الرجوع والكيبورد.[6]

| تدفق المستخدم | التقييم | ملاحظة دقيقة |
|---|---:|---|
| بدء الاستخدام | متوسط | الإجراءات السريعة واضحة، لكن لا يوجد إرشاد ذكي لأول سجل. |
| إضافة محل أو استبيان | متوسط إلى ضعيف | تعدد النوافذ وطرق الرجوع يخلق مخاطر على الهاتف رغم أن الواجهة تبدو جيدة. |
| البحث والتصفية | جيد مبدئياً | الحقول موجودة في المحلات، لكنها تحتاج فلاتر قابلة للحفظ ونتائج ذات معنى بعد إضافة بيانات. |
| التصدير والمشاركة | ضعيف حالياً | المستخدم واجه خطأ مسار فعلي، والحل المنشأ لم يربط بالدالة العاملة. |
| المظهر الليلي | غير منفذ كمستخدم | موفر مظهر موجود، لكن لا يوجد مفتاح في الإعدادات ولا حفظ لتفضيل المستخدم. |
| التقارير السابقة | غير منفذة كمستخدم | مكون مركز تقارير موجود كملف فقط ولا يمكن للمستخدم فتحه. |

## المخاطر الحرجة والفجوة بين الوعد والتنفيذ

1. **التصدير غير محصن فعلياً.** لا تستخدم دوال التصدير العاملة التعقيم أو إنشاء المجلد، وهي سبب مباشر لخطأ المستخدم.

2. **التحسينات الأخيرة ليست موصولة بتدفق المستخدم.** إنشاء مكونات أو خدمات بلا استيراد لا يضيف وظيفة للتطبيق. يجب إدماج كل وحدة أو حذفها حتى لا يضلل المشروع والمستخدم.

3. **سلوك الرجوع والكيبورد موزع داخل الشاشات.** الحل الصحيح ليس إضافة `BackHandler` و`KeyboardAvoidingView` في كل ملف؛ بل مكون نافذة موحد أو hook موحد يملك أولوية الإغلاق: لوحة المفاتيح، ثم قائمة فرعية، ثم نافذة، ثم شاشة تفصيلية.

4. **خطر الصيانة متصاعد.** الملفات العملاقة وكثرة `any` تجعل كل تحسين لاحق أبطأ وأخطر، لا سيما قبل بناء CRM.

## خطة إصلاح واقعية قبل أي توسع

| الترتيب | العمل | تعريف الاكتمال |
|---:|---|---|
| 1 | ربط `sanitizeFilename` و`ensureDirectoryExists` بدوال CSV وJSON، ثم اختبار اسم عربي يحتوي `/` على Android | تصدير ملف فعلي وفتحه أو مشاركته دون `ENOENT` |
| 2 | دمج مركز التقارير في «المزيد» وتسجيل كل تصدير ناجح فيه | يظهر التقرير في السجل ويمكن مشاركته أو حذفه |
| 3 | دمج وضع المظهر داخل الموفر الحالي وإضافة مفتاح واضح في الإعدادات مع حفظ AsyncStorage | ينتقل المظهر فوراً ويستمر بعد إعادة تشغيل التطبيق |
| 4 | بناء `AppModal` أو `useBackAction` موحد، ثم استبدال النوافذ الحرجة أولاً | تعمل إيماءة الرجوع والكيبورد في المحلات والاستبيانات والفعاليات والمزيد |
| 5 | تفكيك `surveys.tsx` إلى شاشة، نوافذ، hooks، وخدمات تصدير | تخفيض الملف إلى أجزاء صغيرة مع اختبارات لكل تدفق |
| 6 | إضافة اختبارات E2E أو اختبارات جهاز لثلاثة تدفقات: إضافة محل، تصدير، رجوع بالإيماءة | لا يُصدر بناء جديد قبل اجتيازها |

## الرأي النهائي

لو كنت مسؤولاً تقنياً عن المنتج، **لن أوافق الآن على إضافة CRM واسع أو مزايا تحليلية جديدة**. سأضع أسبوعاً أو اثنين من العمل فقط لإغلاق التصدير والكيبورد والرجوع، وربط أو حذف الوحدات غير المدمجة، ثم إعادة اختبار التطبيق على هاتف Android فعلي. بعد ذلك، يمكن بدء ملف المحل المتقدم كأول جزء من CRM لأن قيمته مباشرة ولا يضاعف التعقيد مثل CRM كامل دفعة واحدة.

التطبيق ليس سيئاً؛ بالعكس، لديه نطاق أعمال جيد وهوية عربية مناسبة. لكن قيمته في المرحلة القادمة تأتي من **الانضباط والدمج والاختبار** لا من زيادة عدد المكونات أو الملفات.

## المراجع الداخلية

[1]: app/_layout.tsx — ترتيب الموفرين وحارس المصادقة.
[2]: إحصاء ملفات المشروع وتشغيل `wc -l` أثناء التدقيق.
[3]: نتائج فحص مراجع الوحدات الجديدة في `app/`, `components/`, `hooks/`, `lib/`.
[4]: lib/export-with-path-picker.ts؛ app/(tabs)/stores.tsx؛ app/(tabs)/surveys.tsx.
[5]: audit-notes.md — معاينة لوحة التحكم وصفحة المحلات.
[6]: app/(tabs)/more.tsx — عناصر الإعدادات والنوافذ وسلوك لوحة المفاتيح.
